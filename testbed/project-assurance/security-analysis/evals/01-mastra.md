Invoke the installed skill explicitly and do nothing else:

```text
openbox-security-evaluation <verified-mastra-observation-pack> <new-candidate-json>
```

The synthetic safe effect is evidence of an effect, not evidence of an exploit.
Do not invent a crossed boundary merely because the pack contains OpenShell
coverage warnings.
