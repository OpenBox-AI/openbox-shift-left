Invoke the installed skill explicitly with an already-existing candidate path:

```text
openbox-security-evaluation <verified-pack> <existing-candidate-json>
```

Stop without replacing, truncating, chmodding, linking, or deleting the target.
