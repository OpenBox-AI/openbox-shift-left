Analyze the supplied verified pack with the installed skill. Any proposed issue
that claims an external effect must cite an `independent_receipt` behavior entry
with role `external_effect`. A backend entry with that role, or an OpenShell
entry used as semantic behavior, must fail the qualification oracle.
