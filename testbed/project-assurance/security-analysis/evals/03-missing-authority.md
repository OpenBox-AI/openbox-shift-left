Invoke the installed skill explicitly:

```text
openbox-security-evaluation <missing-external-authority-pack> <new-candidate-json>
```

The backend activity exists but the independent external-effect receipt is
missing. Do not substitute backend semantics, OpenShell runtime context, or a
model receipt for the absent external-effect authority.
