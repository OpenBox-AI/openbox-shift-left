assignment_id: "create-epic-20260707-0425"
role: "product_manager (Paul)"
status: "completed"
summary: >
  Sliced the Phase-1 PRD into 3 build-path epics (E1 provider-agnostic core, E2
  openbox CLI + Claude Code adapter, E3 git action) plus 3 deferred/fast-follow
  epics (E5 Codex, E4 Cursor, E6 Phase-2 enforcement). Every Phase-1 FR maps to
  exactly one epic (coverage table in PRD §8); no requirement uncovered or
  duplicated. Epics grouped by coherent outcome and aligned to the architecture
  §1b core+adapter split so write scopes stay disjoint. One prioritization decision
  elicited and recorded — OD13: Codex is the next adapter after Claude Code (not
  Cursor), reflecting S5's finding that Codex is the stronger, lower-risk surface.
artifacts:
  created_or_updated:
    - ".fab7/sdlc/design/prd.md"
  proposed_only: []
write_scope_used:
  - ".fab7/sdlc/design/prd.md"
  - ".fab7/sdlc/assignments/create-epic-20260707-0425.result.yaml"
validation:
  ran:
    - command: "coverage check: every FR (FR-1..FR-8) mapped to exactly one epic"
      result: "pass"
      evidence: "PRD §8 requirement→epic coverage table; FR-1/4/7/8→E1, FR-2/3/5→E2, FR-6→E3"
    - command: "disjoint-write-scope check against architecture §1b split"
      result: "pass"
      evidence: "E1 backend core, E2 client CLI+plugin, E3 CI git action — no overlap; E1→E2→E3 dependencies noted not overlaps"
  not_run:
    - command: "prd-trace"
      reason: "formal FR→test map runs during/after story authoring"
findings:
  - classification: "observation"
    severity: "high"
    target: "epic split"
    detail: "E1 core is the reusable substrate; E2/E4/E5 are adapters over the same contract (FR-4). Build E1 (contract+endpoint) before E2 emitters"
    recommendation: "Sequence E1 → E2 → E3; E4/E5 fast-follow; E6 Phase-2"
  - classification: "decision_needed"
    severity: "medium"
    target: "OD13 fast-follow order"
    detail: "DECIDED: Codex (E5) before Cursor (E4) — S5 shows Codex stronger/lower-risk than beta/fail-open Cursor; revises pre-S5 assumption"
    recommendation: "Plan E5 as the second adapter"
  - classification: "defer"
    severity: "medium"
    target: "E6 Phase-2 enforcement"
    detail: "Out of Phase-1 scope; blocked on spike S2 + OD6"
    recommendation: "Do not slice E6 until S2 lands"
open_decisions:
  - "OD6 (owner: brian): Phase-2 enforcement hook handler type — gate on spike S2 (E6 only)"
  - "OD10 (owner: brian): name pilot team/repo — before rollout stories"
  - "Legal (owner: brian → counsel): DPIA/residency — before launch"
handoff:
  next_phase: >
    plan (slice-stories / draft-story via Sol). Recommend a create-prd/doc-sync pass
    first to align PRD §1-6 with architecture §1b (generic adapter model, OD12, Codex)
    per the readiness finding. Then decompose E1 → E2 → E3 into stories with disjoint
    write scopes; E5/E4 fast-follow; E6 blocked on S2.
