assignment_id: "brainstorm-plugin-20260708-0900"
role: "discovery_analyst (Mara)"
status: "completed"
summary: >
  Brainstorm 002 — packaging shift-left as a native per-tool plugin. Structured
  option set (A plugin-first / B CLI-first / C hybrid / D marketplace channel /
  E MCP-in-plugin / F skills-agents) with tradeoffs vs 6 criteria, evidence (S1/S5)
  separated from assumption, and 4 discarded ideas. Two direction decisions
  elicited and recorded: OD18 = hybrid (plugin vehicle + CLI engine; revises OD12),
  OD19 = Phase-1 plugin bundles bin/openbox + hooks only. Propagated to
  architecture (OD12 revised; OD18/19/20 added; §1b front-door updated) and the
  shift-left backlog (SL-2 installs plugin; SL-4 = the CC plugin).
artifacts:
  created_or_updated:
    - ".fab7/sdlc/discovery/brief.md"
    - ".fab7/sdlc/design/architecture.md"
    - ".fab7/sdlc/stories/shift-left-backlog.md"
  proposed_only: []
write_scope_used:
  - ".fab7/sdlc/discovery/brief.md"
  - ".fab7/sdlc/design/architecture.md"
  - ".fab7/sdlc/stories/shift-left-backlog.md"
  - ".fab7/sdlc/assignments/brainstorm-plugin-20260708-0900.result.yaml"
validation:
  ran:
    - command: "grounding check vs S1 A2 (CC plugin bundles bin/hooks/MCP/agents; marketplace; managed enabledPlugins) and S5 §3 (Codex requirements.toml/MDM)"
      result: "pass"
      evidence: "brief Brainstorm 002 evidence block, cited"
  not_run: []
findings:
  - classification: "decision_needed"
    severity: "high"
    target: "OD18 delivery model"
    detail: "DECIDED hybrid — CC plugin bundles bin/openbox (Go, OD17) + hooks; openbox dev init installs it; CLI stays uniform front door + CI/git-action path. Revises OD12."
    recommendation: "Refine SL-2 (install plugin) and SL-4 (build CC plugin) scope; no new stories"
  - classification: "decision_needed"
    severity: "medium"
    target: "OD19 plugin contents"
    detail: "DECIDED Phase-1 bundle = bin/openbox + hooks only; MCP server (Phase-2 candidate) and skills/agents deferred"
    recommendation: "Keep observe-first bundle minimal"
  - classification: "observation"
    severity: "medium"
    target: "cross-tool asymmetry"
    detail: "Only Claude Code has a rich plugin bundle; Codex/Cursor are config+managed-hooks bundles — 'plugin' is CC-specific, the generic concept is 'installable governance bundle per tool'"
    recommendation: "Keep the generic adapter contract (§1b); package natively per tool"
open_decisions:
  - "OD20 (owner: brian): distribution channel (private CC marketplace + managed enabledPlugins; Codex requirements.toml/MDM; Cursor Team hooks) — confirm org enterprise tiers; ties OD10"
handoff:
  next_phase: >
    Design updated. Continue build: STORY-SL-3 (client) is ready and unblocked;
    SL-2/SL-4 scope now reflects the plugin-vehicle model. Optionally revise the
    STORY-SL-2/STORY-SL-4 contracts to state plugin packaging explicitly before they build.
