{
  "//": "OpenBox observe-only hooks (STORY-SL-4; unified engine STORY-SL4-WIRE-2). Each hook shells out to the bundled Go engine as `bin/openbox hook claude-code <event>`, which maps the payload to a normalized OpenBox event and spools it locally (no network on the hot path). The hook always exits 0 with empty stdout, so it can NEVER block a tool call or inject context (INV-3 / D7). SessionEnd additionally flushes the spool to OpenBox. The event name is passed as the argv subcommand (deterministic, independent of the payload).",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "",
        "hooks": [
          { "type": "command", "command": "\"${CLAUDE_PLUGIN_ROOT}/bin/openbox\" hook claude-code SessionStart", "timeout": 5 }
        ]
      }
    ],
    "UserPromptSubmit": [
      {
        "matcher": "",
        "hooks": [
          { "type": "command", "command": "\"${CLAUDE_PLUGIN_ROOT}/bin/openbox\" hook claude-code UserPromptSubmit", "timeout": 5 }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "*",
        "hooks": [
          { "type": "command", "command": "\"${CLAUDE_PLUGIN_ROOT}/bin/openbox\" hook claude-code PreToolUse", "timeout": 5 }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "*",
        "hooks": [
          { "type": "command", "command": "\"${CLAUDE_PLUGIN_ROOT}/bin/openbox\" hook claude-code PostToolUse", "timeout": 5 }
        ]
      }
    ],
    "SessionEnd": [
      {
        "matcher": "",
        "hooks": [
          { "type": "command", "command": "\"${CLAUDE_PLUGIN_ROOT}/bin/openbox\" hook claude-code SessionEnd", "timeout": 15 }
        ]
      }
    ]
  }
}
